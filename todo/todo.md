[] Extract TownOverlay, MenuPanel, CatPanel from main.ts into separate modules (deferred — functional but large)
[] Generate interaction animations (scratch, sit, eat) for non-wildcat breeds (requires PixelLab MCP)
